{
    "version": 2,
    "name": "intesa-vincente",
    "buildCommand": "npm run build",
    "installCommand": "npm install",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "dist/main.js",
            "methods": ["GET", "POST", "PUT", "DELETE"]
        }
    ]
}